import { styled } from '@material-ui/core/styles';
import { Container } from '@material-ui/core';

export const MyContiner = styled(Container)({
    maxWidth: '800px',
    minHeight: '90vh',
    display: 'flex',
    flexWrap: 'wrap',
});
